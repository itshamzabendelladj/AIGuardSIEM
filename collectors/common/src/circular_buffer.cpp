#include "aiguard/common/circular_buffer.h"

// Template instantiations for common types
// (Lock-free buffer is header-only; this file provides any non-template utilities)

namespace aiguard {
// Currently no non-template implementations needed
// Template instantiations happen at use site
} // namespace aiguard
